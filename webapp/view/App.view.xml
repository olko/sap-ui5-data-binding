<mvc:View
    controllerName="sap.ui.demo.db.controller.App"
    xmlns="sap.m"
    xmlns:form="sap.ui.layout.form"
    xmlns:l="sap.ui.layout"
    xmlns:mvc="sap.ui.core.mvc">
    <Panel headerText="{i18n>panel1HeaderText}"
           class="sapUiResponsiveMargin"
           width="auto">
        <form:SimpleForm
            editable="true"
            layout="ColumnLayout">
            <Label text="{i18n>firstName}" />
            <Input
                value="{/firstName}"
                valueLiveUpdate="true"
                width="200px"
                enabled="{/enabled}" />
            <Label text="{i18n>lastName}" />
            <Input
                value="{/lastName}"
                valueLiveUpdate="true"
                width="200px"
                enabled="{/enabled}" />
            <Label text="{i18n>enabledText}" />
            <CheckBox selected="{/enabled}" />
        </form:SimpleForm>
        <Panel headerText="{i18n>panel2HeaderText}">
            <content>
                <l:VerticalLayout>
                    <Label
                        text="{i18n>address}"
                        labelFor="address"
                        />
                    <FormattedText
                        htmlText="{/address/street}&lt;br&gt;{/address/zipcode} {/address/city}&lt;br&gt;{/address/country}"
                        class="sapUiResponsiveMargin"
                        width="200px"
                        id="address"
                        />
                    <Link
                        href="{
                            parts: ['/firstName', '/lastName'],
                            formatter: '.formatMail'
                        }"
                        text="Email"
                        />
                </l:VerticalLayout>
                <l:VerticalLayout>
                <Label
                    labelFor="salesAmount"
                    text="{i18n>salesAmount}"
                    />
                <Input
                    description="{/currencyCode}"
                    enabled="{/enabled}"
                    editable="false"
                    id="salesAmount"
                    value="{
                            parts: [
                                { path: '/salesAmount' },
                                { path: '/currencyCode' }
                            ],
                            type: 'sap.ui.model.type.Currency',
                            formatOptions: {
                                preserveDecimals: false,
                                showMeasure : false
                            }
                         }"
                    />
                </l:VerticalLayout>
            </content>
        </Panel>
    </Panel>
</mvc:View>
